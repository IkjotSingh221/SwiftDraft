# Related Work

Prior work on hybrid retrieval established strong baselines for combining
dense and sparse signals [@smith2019]. Our approach builds directly on that
line of work, but the following marker is malformed and should be flagged
[@bad key], as should this empty one [@].
