"""Phase 6: deterministic rendering (`assemble.py`) and the Pandoc subprocess
wrapper (`pandoc.py`). See each module's docstring; spec.md non-negotiable #1
("assembly is deterministic file concatenation + Pandoc, never an LLM call")
applies to everything in this package -- no LLM call is ever made here.
"""
