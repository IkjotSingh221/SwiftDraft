import PhasePlaceholder from "../components/PhasePlaceholder";

export default function OutlineReview() {
  return (
    <PhasePlaceholder
      title="Outline Review"
      phase="Phase 3"
      description="A collapsible outline tree with inline-editable titles, briefs, and word targets, plus an approve action that resumes the graph, arrives in Phase 3."
    />
  );
}
