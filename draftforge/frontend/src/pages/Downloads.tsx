import PhasePlaceholder from "../components/PhasePlaceholder";

export default function Downloads() {
  return (
    <PhasePlaceholder
      title="Downloads"
      phase="Phase 6"
      description="Rendered docx/PDF, bibliography, eval report, and decisions log downloads arrive in Phase 6 (eval report added in Phase 7)."
    />
  );
}
