import PhasePlaceholder from "../components/PhasePlaceholder";

export default function RunDashboard() {
  return (
    <PhasePlaceholder
      title="Run Dashboard"
      phase="Phase 4"
      description="Live per-section status via SSE, a running token/cost counter, and a per-section decisions log viewer arrive in Phase 4."
    />
  );
}
