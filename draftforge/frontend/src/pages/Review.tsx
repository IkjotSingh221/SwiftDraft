import PhasePlaceholder from "../components/PhasePlaceholder";

export default function Review() {
  return (
    <PhasePlaceholder
      title="Review"
      phase="Phase 5"
      description="Flagged citations (claim, chunk, verifier verdict) with accept/redraft actions, and continuity-editor diffs per boundary, arrive in Phase 5."
    />
  );
}
