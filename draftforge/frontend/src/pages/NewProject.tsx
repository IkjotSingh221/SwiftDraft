import PhasePlaceholder from "../components/PhasePlaceholder";

export default function NewProject() {
  return (
    <PhasePlaceholder
      title="New Project"
      phase="Phase 1"
      description="Drag-and-drop source upload, format spec picker, and per-file ingestion progress land here in Phase 1."
    />
  );
}
